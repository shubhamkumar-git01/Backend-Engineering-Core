// Topic: Advanced Querying with Mongoose

// const User = require('./userModel');

/*
// CREATE
const newUser = await User.create({ name: 'Shubham', email: 'test@test.com' });

// READ
const users = await User.find({ role: 'admin' });
const user = await User.findById(req.params.id);

// UPDATE
const updatedUser = await User.findByIdAndUpdate(req.params.id, req.body, {
    new: true, // returns the updated document
    runValidators: true // runs schema validation again
});

// DELETE
await User.findByIdAndDelete(req.params.id);
*/
console.log("Check comments for Mongoose CRUD operations");
